# Raw generated art. These are concept sources, not sprites — hundreds of KB
# each, and ~100x heavier than what ships. `npm run art:clean` reduces them
# into public/, and those cleaned sprites are what is committed.
art-source

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage
coverage
playwright-report
test-results

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local
